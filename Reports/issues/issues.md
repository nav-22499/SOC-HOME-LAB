
### Issue Encountered
The nano editor was not installed by default on the Ubuntu Server minimal installation.

Resolution:
Installed nano using apt package manager.

Commands Used:
sudo apt update
sudo apt install nano -y
